# <项目名>：项目总览调研

> 状态：调研草稿  
> 代码库位置：`<本地路径>`  
> 阅读日期：`<YYYY-MM-DD>`  
> 主要关注：项目定位、入口文件、整体结论

> 注意：本文件只作为单项目 `README.zh-CN.md` 或总览笔记模板。正式分析必须拆成：
>
> - `user-adaptation-system.zh-CN.md`
> - `memory-system.zh-CN.md`

## 0. 一句话定位

这个项目主要在解决：

> `<用一两句话说明项目自己的核心问题，不要先套 Aether 术语。>`

## 1. 项目自己的核心概念

| 名称 | 在项目里的含义 | 接近 Aether 的什么 |
| --- | --- | --- |
| `<term>` | `<含义>` | `<可为空，不强行对应>` |

## 2. 主要用户流程

```text
<用户如何开始使用 -> 系统做什么 -> 输出什么结果>
```

## 3. 主要程序流程

```text
<输入 -> 处理 -> 存储/检索 -> 输出>
```

## 4. 关键入口文件

| 文件 | 为什么重要 | 已读状态 |
| --- | --- | --- |
| `<path>` | `<reason>` | `<read/unread>` |

## 5. 正式分析文件

本项目必须继续拆分为：

- [ ] `user-adaptation-system.zh-CN.md`
- [ ] `memory-system.zh-CN.md`

## 6. 初步优点

- `<优势 1>`
- `<优势 2>`
- `<优势 3>`

## 7. 初步顾虑

- `<风险 1>`
- `<风险 2>`
- `<风险 3>`

## 8. 待继续验证的问题

- `<问题>`
