# <项目名>：用户自适应系统审阅

> 状态：调研草稿  
> 项目路径：`/home/bzz/Aether/reference_project/<project-name>`  
> 输出文件：`docs/IPK/reference_project_review/<project-name>/user-adaptation-system.zh-CN.md`

## 0. 先给结论

这个项目是否有明确用户自适应系统：

> `<有 / 部分有 / 几乎没有 / 不是它的重点>`

一句话说明：

> `<不要用 Aether 术语硬套，先用项目自己的语言概括。>`

## 1. 项目自己的用户需求判断

这个项目似乎认为用户真正需要：

- `<需求 1>`
- `<需求 2>`
- `<需求 3>`

这些判断来自：

| 证据 | 位置 | 说明 |
| --- | --- | --- |
| 文档 / 代码 / 测试 | `<path>` | `<说明>` |

## 2. 它把用户自适应叫什么

| 项目术语 | 含义 | 是否等同 Aether 的用户画像 / 习惯 |
| --- | --- | --- |
| `<term>` | `<meaning>` | `<yes/no/部分相似>` |

## 3. 用户信息从哪里来

| 来源 | 是否用户显式提供 | 是否模型自动提取 | 是否有证据保存 | 风险 |
| --- | --- | --- | --- | --- |
| `<source>` | `<yes/no>` | `<yes/no>` | `<yes/no>` | `<risk>` |

## 4. 用户信息怎样影响行为

```text
<用户输入 / session / profile / retrieval / prompt / tool / response 的流程图>
```

说明：

- `<它在什么时机读取用户信息。>`
- `<用户信息交给哪个模块。>`
- `<最终改变了什么行为。>`

## 5. 写入、更新、纠正和删除

| 动作 | 触发条件 | 用户是否确认 | 关键代码 / 文档 | 评价 |
| --- | --- | --- | --- | --- |
| 写入 | `<trigger>` | `<yes/no>` | `<path>` | `<comment>` |
| 更新 | `<trigger>` | `<yes/no>` | `<path>` | `<comment>` |
| 纠正 | `<trigger>` | `<yes/no>` | `<path>` | `<comment>` |
| 删除 | `<trigger>` | `<yes/no>` | `<path>` | `<comment>` |

## 6. 作用域和边界

它是否区分：

- 全局用户偏好
- 项目级偏好
- 会话内临时偏好
- 文件 / artifact 规则
- 主题 / 学科偏好
- agent 自己的经验
- 外部知识内容

结论：

> `<从项目自身角度解释它为什么这样分或为什么不分。>`

## 7. 优点

先写项目自己的优点，不要先写“可借鉴到 Aether”。

- `<优点 1>`
- `<优点 2>`
- `<优点 3>`

## 8. 顾虑

- `<顾虑 1>`
- `<顾虑 2>`
- `<顾虑 3>`

## 9. 对 Aether 旧设计的挑战

| Aether 旧前提 | 是否被支持 / 挑战 / 补充 | 说明 |
| --- | --- | --- |
| `<old assumption>` | `<supported/challenged/expanded>` | `<reason>` |

## 10. 对 Aether 的可能改变

这里允许写根本改变，不只写实现技巧。

### 10.1 用户需求理解的改变

- `<change>`

### 10.2 产品流程的改变

- `<change>`

### 10.3 程序架构的改变

- `<change>`

### 10.4 只适合保留为启发的点

- `<point>`

## 11. 仍需继续读的文件

- `<path>`：`<为什么还要读>`
