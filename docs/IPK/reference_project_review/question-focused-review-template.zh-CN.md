# <问题标题>：问题驱动批量复审报告模板

> 状态：调研草稿  
> 输出目录：`docs/IPK/reference_project_review/question_reviews/<YYYY-MM-DD>-<question-slug>/`  
> 项目范围：`/home/bzz/Aether/reference_project`，默认排除 `andrej-karpathy-skills`、`darwin_skill`、`docs`

## 0. 用户问题

原始问题：

> `<粘贴用户问题>`

本次审阅主题：

> `<把用户问题压缩成一句可审阅主题>`

## 1. 问题轴

| 轴 | 要判断什么 | 为什么重要 |
| --- | --- | --- |
| 1 | `<axis>` | `<reason>` |
| 2 | `<axis>` | `<reason>` |
| 3 | `<axis>` | `<reason>` |

## 2. 项目范围

| 项目 | 路径 | 状态 | 备注 |
| --- | --- | --- | --- |
| `<project>` | `<path>` | `<done/pending/skipped>` | `<note>` |

## 3. 跨项目结论

### 3.1 多数项目共同模式

- `<pattern>`

### 3.2 高价值反例

- `<counterexample>`

### 3.3 仍然冲突的做法

- `<conflict>`

### 3.4 证据不足的区域

- `<gap>`

## 4. 每项目摘要

| 项目 | 对本问题的答案 | 关键证据 | 对 Aether 的挑战 |
| --- | --- | --- | --- |
| `<project>` | `<answer>` | `<path / docs>` | `<impact>` |

## 5. 对 Aether 的影响

### 5.1 可能立即吸收

- `<implementation or workflow idea>`

### 5.2 当前 Aether 值得保留

- `<keep>`

### 5.3 当前 Aether 可能需要更改

- `<change>`

### 5.4 需要重新讨论

- `<product / architecture assumption>`

### 5.5 可能改写权威文档

- `<doc and reason>`

### 5.6 需要用户实验

- `<experiment question>`

## 6. Open Questions 清理

| 项目 | 处理 |
| --- | --- |
| 已解决的问题 | `<list or none>` |
| 需要改写的问题 | `<list or none>` |
| 新增的问题 | `<list or none>` |
| 保持不变的问题 | `<list or none>` |

## 7. 后续行动

- `<next step>`
